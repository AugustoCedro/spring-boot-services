version: '3.8'

services:
  db:
    image: leogloriainfnet/postgres-tp2-spring:1.0
    container_name: tp2-spring
    environment:
      POSTGRES_DB: guilda_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: adm
    ports:
      - "5432:5432"
